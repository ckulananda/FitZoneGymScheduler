<Window x:Class="FitZoneGymScheduler.Views.AboutDeveloperWindow"
         Icon="/Assets/icon.ico"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About Developer"
        Width="900"
        Height="650"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#0F172A">

    <Window.Resources>

        <!-- Button Style -->
        <Style x:Key="SocialButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#1E293B"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#7C3AED"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <Grid Margin="20">

        <Border CornerRadius="18"
                Background="#111827"
                Padding="20">

            <Grid>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- HEADER -->
                <Border CornerRadius="15"
                        Background="#1E293B"
                        Padding="20"
                        Margin="0,0,0,15">

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- PROFILE IMAGE -->
                        <Border Width="110"
                                Height="110"
                                CornerRadius="55"
                                BorderBrush="#7C3AED"
                                BorderThickness="2"
                                HorizontalAlignment="Left">

                            <Image Source="/Assets/Developer.jpg"
                                   Stretch="UniformToFill"/>
                        </Border>

                        <!-- NAME -->
                        <StackPanel Grid.Column="1"
                                    VerticalAlignment="Center"
                                    Margin="15,0,0,0">

                            <TextBlock Text="Dadallage Chamodha Dhananjana Kulananda"
                                       Foreground="White"
                                       FontSize="20"
                                       FontWeight="Bold"/>

                            <TextBlock Text="Software Engineering &amp; BIS Undergraduate"
                                       Foreground="#CBD5E1"
                                       Margin="0,5,0,0"/>

                            <TextBlock Text="Southern Province, Matara, Sri Lanka"
                                       Foreground="#94A3B8"
                                       Margin="0,5,0,0"/>
                        </StackPanel>
                    </Grid>

                </Border>

                <!-- CONTACT -->
                <StackPanel Grid.Row="1"
                            Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            Margin="0,0,0,15">

                    <TextBlock Text="📧  dhananjana.kulananda@gmail.com"
                               Foreground="#CBD5E1"
                               Margin="10"/>

                    <TextBlock Text="📞  +94 70 104 4091"
                               Foreground="#CBD5E1"
                               Margin="10"/>

                </StackPanel>

                <!-- CONTENT -->
                <Grid Grid.Row="2">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- ABOUT -->
                    <Border Background="#1E293B"
                            CornerRadius="15"
                            Padding="15"
                            Margin="0,0,10,0">

                        <StackPanel>

                            <TextBlock Text="Professional Summary"
                                       Foreground="White"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       Margin="0,0,0,10"/>

                            <TextBlock TextWrapping="Wrap"
                                       Foreground="#CBD5E1"
                                       Text="Enthusiastic BIS undergraduate with experience in desktop, web and mobile development. Passionate about building technology-driven business solutions and improving software systems."/>
                        </StackPanel>

                    </Border>

                    <!-- LINKS + SKILLS -->
                    <Border Grid.Column="1"
                            Background="#1E293B"
                            CornerRadius="15"
                            Padding="15"
                            Margin="10,0,0,0">

                        <StackPanel>

                            <TextBlock Text="Links"
                                       Foreground="White"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       Margin="0,0,0,10"/>

                            <Button Content="GitHub"
                                    Style="{StaticResource SocialButtonStyle}"
                                    Margin="0,0,0,8"
                                    Click="GitHub_Click"/>

                            <Button Content="LinkedIn"
                                    Style="{StaticResource SocialButtonStyle}"
                                    Margin="0,0,0,15"
                                    Click="LinkedIn_Click"/>

                            <TextBlock Text="Technical Skills"
                                       Foreground="White"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       Margin="0,10,0,10"/>

                            <TextBlock Text="C# • WPF • SQL Server • .NET • Android • Git • MVVM • QGIS • R"
                                       Foreground="#CBD5E1"
                                       TextWrapping="Wrap"/>

                        </StackPanel>

                    </Border>

                </Grid>

            </Grid>

        </Border>

    </Grid>

</Window>